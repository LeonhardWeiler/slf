1. selection invert, also dass bei der selection color in allen modernen
   browser der background die davorige textcolor ist und die textcolor die
davorige backgroundcolor
2. camera access request ios und android, also dass der user nach den browser
   berechtigungen gefragt wird, wenn er diese nicht gesetzt hat
3. lobbycode to copy immer nicht nur im lobby screen zu sehen und zu kopieren,
   es steht oben neben runde läuft
4. tab reihenfolge zuerst auf den inputs und dann erst auf den flammen
5. es wird er beim host angezeigt, dass es fertig ist, wenn der zod test am
   server passt, also "arst" bei buchstabe "o" zeigt nicht done an beim host,
wenn er moderator ist
6. die flamme wird im review screen nicht beim namen sondern neben der answer
   angezeigt

