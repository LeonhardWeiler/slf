1. Beim Login Screen sollen nur 2 buttons zu sehen sein mit create und
   join. Wenn man auf create clickt kommt ein input name feld und dann
wenn man auf weiter clickt, wird die lobby erstellt. bei join soll zuerst ein
input feld kommen in dem man den code eingeben kann und dann klickt man auf
weiter und kann den namen eingeben und dann joint man der lobby.

2. Außerdem soll auf der startseite ein scan qr button sein, über dem man
   auch die lobby beitreten kann. es passiert dann das gleiche als hätte
man beim join button schon den lobbycode eingegeben, man braucht also
nur mehr den namen und kann dann joinen. Der QR code soll über einen show qr
button in der lobby sichtbar sein. Außerdem soll man den link zum joinen
angezeigt werden und mit click darauf kopiert werden, das ist genau der link
auf dem man mit dem qr code kommt

3. Der Toggle button in der lobby für den host ist nicht richtig
   gestyled und im darkmode komplett weiß, das muss geändert werden, dass
   man toggle status sieht

4. die zeit anzeige funktioniert nicht im gestartete spiel wenn man einen
   countdown hat, ich sehe keinen timer und auch wenn ich kein zeit limit
habe, soll die vergangene zeit (stoppuhr) angezeigt werden. die zeit
funktionert aber jetzt grad trotzem schon, also es geht nach der zeit die
runde vorbei. Ich hätte aber gerne, dass in den letzten 5s der bildschirmrand
rot wird. gerade funktioniert diese anzeige mit dem timer nur bei refresh

5. in der großen ui sollen die felder wie bei einem klassischen
   stadt land fluss spiel nebeneinander in spalten angezeigt werden, in
mobile passt es aber wie es ist.

6. In der analyse soll zum entfernen kein haken, sondern ein x sein und
   umgekehrt, also die icons sind genau verkehrt wie es sinn macht.

7. das zusammenführen soll nicht über ein dropdown sondern über einen
   button und dann wenn man das nächste anklickt soll es
zusammengeführt werden. nicht mehr zusammenführen soll man über einen
disconnect button

8. der host soll mit pfeiltasten in der analysefunktion nach der runde vor
   und zurück gehen können

9. bevor die runde startet wird zuerst die andere ui gestartet und es schaut
   deshalb ein bisschen wie ein ui bug aus. der countdown soll sofort
angezeigt werden

10. der client soll eingaben überprüfen und nur wenn diese mit zod am client
    verified sind, soll man buzzern können. falsche inputs (falscher
anfangsbuchstabe) können nicht zum buzzern verwendet werden.

11. wenn ich leere inputs mach, dann schaut es wie dann im review screen
    aus als hätte es eine abgabe gegeben, obwohl keine angezeigt
werden soll

12. Wenn das alphabet leer ist oder das spiel beendet wird, sollen alle
    wieder in der start lobby screen kommen und die runde soll noch nicht
    sofort neugestartet werden

13. wenn ein spieler die runde verlässt, sollen immer noch seine punkte
    angezegit werden und er soll als spieler sichtbar sein. als er soll
    in der tabelle noch weiterhin angezeigt werden, aber nicht mehr im
review. Falls das keinen sinn macht, frage nach
